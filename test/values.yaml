# 副本数
replicaCount: 2

# 镜像配置
image:
  repository: nginx   # 镜像名
  tag: "latest"       # 镜像 Tag
  pullPolicy: IfNotPresent

# 服务配置
service:
  type: ClusterIP     # 或者是 NodePort, LoadBalancer
  port: 80            # 服务暴露的端口
  targetPort: 80      # 容器内部监听的端口